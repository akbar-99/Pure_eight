{
  "name": "pure-eight",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:web": "turbo dev --filter=@pure-eight/web",
    "dev:staff": "turbo dev --filter=@pure-eight/mobile-staff",
    "dev:customer": "turbo dev --filter=@pure-eight/mobile-customer"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5"
  },
  "packageManager": "npm@10.9.2",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "react-native-worklets": "^0.9.1"
  },
  "//optionalDependencies": "npm prunes non-matching platform binaries from the lockfile (npm/cli#4828), so a lockfile generated on macOS ships no Linux bindings and CI cannot build. lightningcss is hoisted to the root node_modules, so its binding must be declared here rather than in apps/web. os/cpu constraints mean these only install on Linux.",
  "optionalDependencies": {
    "lightningcss-linux-x64-gnu": "1.32.0",
    "lightningcss-linux-x64-musl": "1.32.0"
  }
}
